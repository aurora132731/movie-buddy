# Optional: live IMDb / Rotten Tomatoes ratings (local or Vercel env)
OMDB_API_KEY=

# Vercel KV — create a KV database in your Vercel project, then these are set automatically
KV_REST_API_URL=
KV_REST_API_TOKEN=
